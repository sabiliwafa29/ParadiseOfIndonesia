[php]
documentRoot = "public"
extensions = [
  "pgsql",
  "pdo_pgsql",
  "bcmath",
  "ctype",
  "curl",
  "fileinfo",
  "gd",
  "json",
  "mbstring",
  "openssl",
  "pdo",
  "tokenizer",
  "xml"
]

[deploy]
# Use PHP-FPM + Nginx (Nixpacks default for PHP)
# No custom start command needed
